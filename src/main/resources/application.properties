spring.application.name=ttscore-api

# Firebase
app.firebase.service-account=classpath:firebase-service-account.json

app.jwt.secret=${APP_JWT_SECRET}
app.jwt.expiration=86400000

# Server
server.port=8080
